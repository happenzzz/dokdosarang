# 교사 로그인·접속자 수정 검증

## 원인과 수정
기존 규칙은 접속 정보가 아직 없는 경로에 대한 삭제를 거부했습니다. 앱이 첫 위치를 기록하기 전에 onDisconnect().remove()를 예약하므로, 그 예약부터 실패하여 위치를 전송하지 못했습니다. 교사 로그인은 같은 경로의 삭제를 기다리다가 실패해 팝업을 열지 못했습니다.

- 인증된 사용자가 빈 경로의 삭제를 예약할 수 있도록 수정했습니다.
- 기존 접속 정보가 있는 경우 소유자만 수정·삭제할 수 있는 조건과 데이터 검증은 유지했습니다.
- Google 팝업을 클릭 직후 열고, 삭제 실패가 로그인을 차단하지 않도록 수정했습니다.
- 계정 UID 변경 시 새 접속 세션을 만들고, 기존 구독을 정리한 뒤 다시 연결합니다.
- 실제 위치 기록이 실패했을 때 성공 상태로 덮어쓰지 않습니다.

Firebase는 onDisconnect 예약 시와 실행 시에 권한을 확인합니다. 공식 설명: https://firebase.google.com/docs/database/web/offline-capabilities

## 로컬 검증 통과
`node tests/presence-regression.cjs`로 실행할 수 있습니다. 외부 패키지 설치 없이 Node.js를 사용합니다.

- 기존 규칙의 첫 입장 거부 재현 및 수정 규칙의 빈 경로 삭제 예약 허용.
- 인증되지 않은 요청, 다른 사용자 수정·삭제, UID 위조, 잘못된 좌표·임의 필드 거부.
- 24개 모의 클라이언트 각각에서 자기 외 23명의 접속 정보 확인.
- 이동 정보 전달, 접속 종료 정보 제거, 재연결 시 구독 중복 방지.
- 접속 정보 삭제 거부에도 Google 팝업이 즉시 호출됨.
- 익명 계정 연결 및 기존 Google 계정 전환 후 교사 권한·접속 상태 갱신.
- 허용 목록 밖 Google 계정은 교사 권한을 얻지 않고 익명 계정으로 복구.
- 최종 앱 JavaScript 문법 검사 및 규칙 JSON/BOM 검사.

위 테스트는 실제 앱 함수와 규칙 표현식을 실행하는 로컬 모의 테스트입니다. Firebase Emulator의 규칙 컴파일이나 실제 Google 팝업 인증·교내 24대 기기 테스트를 실행한 것은 아닙니다. 두 파일을 배포하고 실제 기기에서 확인해야 합니다.


## 채팅 업데이트 검증
`node tests/chat-regression.cjs` 로컬 모의 테스트 통과:
- 자기 말풍선 즉시 표시, 최신 메시지 교체, 서버 응답 중복 방지.
- 5초 표시 시간 설정, 메시지를 HTML로 실행하지 않고 텍스트로 표시.
- 친구 메시지 수신 및 삭제 시 말풍선 제거, 옛 기록 재표시 방지.
- 전송 실패 시 말풍선 제거 및 입력 복원.
- 학생의 삭제 함수 호출 차단, 교사 개별 삭제.
- 650개 기록을 300개씩 나누어 삭제하고 삭제 시작 후의 새 메시지 보존.
실제 Firebase 및 브라우저 시각 검증은 미실행입니다.
